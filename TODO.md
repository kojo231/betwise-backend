# TODO - Fix backend + get today’s matches

- [x] Diagnose backend failure cause in `app.py`
- [x] Refactor `/api/fixtures` route (it was duplicated and malformed)
- [x] Ensure app imports cleanly and Flask starts
- [x] Add robust error handling + response format guarantees
- [ ] Run the backend and hit `/api/health` to confirm it’s up
- [ ] Call `/api/fixtures` and verify it returns today’s matches
- [ ] (Optional) run a lightweight data fallback if external API fails


